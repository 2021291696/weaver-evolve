# debug-architect — 调试建筑师

项目完成后扫描报错、分析根因、归档教训、生成预防规则、检测技能漏洞。

## 为什么需要

- 项目做到最后，各种报错散落在对话历史里，没人总结
- 同一个坑反复踩，因为没归档、没规则、没预警
- skill 有漏洞但没人发现——直到下一次报错

debug-architect 在项目完成后一次性扫描所有报错，把碎片变成预防体系。

## 功能

1. **扫描提取** — 从 sessions 自动搜索所有报错信号
2. **分类归类** — 语法/配置/逻辑/环境/第三方/其他
3. **根因分析** — 三层：直接原因→深层原因→预防缺口
4. **关联分析** — 因果链、同根多面、跨项目共鸣
5. **置信度分级** — 四象限（确定×复利）分级，不同等级不同处理策略
6. **归档写入** — 按等级写入项目 ERROR_LOG.md + memory/ + wiki/
7. **预防建议** — 按置信度等级决定是否写入 CLAUDE.md 规则
8. **技能漏洞** — 检测规则缺失、边界遗漏、检查缺失

## v2 新增

- **置信度分级**：每个错误按诊断把握 × 长期复利价值分为 🟢🟡🔵⚪ 四级
- **ERROR_LOG**：每个项目自动生成 `.claude/ERROR_LOG.md`，作为免疫记忆
- **验证闭环**：推测性诊断提供验证选项，验证通过自动升级处理等级
- **频率降级**：频率不再是独立规则，而是置信度评估的输入信号

## 触发

```
命令：/error-review
关键词："复盘错误"、"错误复盘"、"分析报错"、"回顾错误"、"总结错误"
```

项目完成后手动调用。也由 [weaver-自我迭代](../weaver-自我迭代/) 在全局整理时自动调用。

## 安装

本 Skill 属于 [weaver-evolve 技能集群](https://github.com/2021291696/weaver-evolve)，推荐通过集群一键安装：

```bash
git clone https://github.com/2021291696/weaver-evolve.git
cd weaver-evolve && bash install.sh   # 或: powershell -File install.ps1
```

单独安装：

## 文件结构

```
debug-architect/
├── SKILL.md                     — 主指令（~304行，九步流程）
├── README.md                    — 本文件
└── references/
    ├── error-patterns.md        — 常见错误模式速查
    └── skill-audit-guide.md     — 技能漏洞检测指南
```

## 与体系内其他 skill 的关系

| | debug-architect | weaver-自我迭代 | memory-keeper |
|------|-----------------|-----------------|---------------|
| 时机 | 项目完成后 | 定期 | 被 debug-architect 调用 |
| 对象 | 错误报告 | 所有对话 | 单条写入 |
| 产出 | 归档+预防规则+技能改进 | 知识网络+经验+自迭代 | memory/wiki 写入 |
